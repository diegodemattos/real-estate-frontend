/**
 * Production environment values.
 *
 * This file is used by default (`ng build` / `ng serve
 * --configuration=production` / `npm start` / `npm run build`).
 *
 * The development configuration replaces this file with
 * `environment.development.ts` via `angular.json` → `fileReplacements`.
 */
export const environment = {
  production: true,
};
