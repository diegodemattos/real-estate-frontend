/**
 * Development environment values.
 *
 * Substituted for `environment.ts` at build time when the `development`
 * configuration is active — `ng build --configuration=development`,
 * `ng serve --configuration=development`, `npm run build:dev`, or
 * `npm run serve:dev`.
 */
export const environment = {
  production: false,
};
